import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper__input__mask: {
    height: 56,
    borderRadius: 99,
    justifyContent: 'center',
  },
  input__mask: {
    textAlign: 'center',
    lineHeight: 22,
    fontFamily:'Manrope-SemiBold',
    fontSize: 14,
    fontWeight: '600',
    color: '#000000',
  },
  wrapper__code__field: {
    justifyContent: 'center',
    width: 40,
    height: 48,
    borderWidth: 1,
    borderRadius: 8,
    borderColor: '#CACACC',
    backgroundColor: '#FFFFFF',
    marginHorizontal: 4
  },
});
