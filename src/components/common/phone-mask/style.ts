import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper__input__mask: {
    flex:1,
    width: "100%",
    backgroundColor: '#FFFFFF',
    justifyContent: 'center',
  },

  input__mask: {
    flex:1,
    textAlign: 'left',
    fontSize: 15,
    fontFamily:'Manrope-SemiBold',
    fontWeight: '600',
    color: '#000000',
    borderColor: "#CACACC",
    padding: 10,
    borderWidth: 1,
    borderRadius: 8,
    paddingLeft: 16,
  },
});
